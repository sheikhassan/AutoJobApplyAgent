{$DOMAIN} {
    encode zstd gzip
    request_body {
        max_size 1MB
    }
    @backend path /api/* /auth/* /health /ready
    handle @backend {
        reverse_proxy backend:8000
    }
    handle {
        reverse_proxy frontend:3000
    }
    header {
        -Server
    }
}
